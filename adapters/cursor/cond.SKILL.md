---
name: cond
description: Short alias for the Condiments token-efficiency command.
disable-model-invocation: true
---

Treat this invocation as `/cond <arguments>`. For a command-only turn, do not read the sibling skill or references. Run `../condiments/scripts/condiments.mjs` immediately with `--host cursor`, sibling `capabilities.json`, and `--prompt-only`; apply its compact delta and stop after success. Omit `--prompt-only` for status or `v|ver|version`. Before substantive work with an active policy, read `../condiments/SKILL.md` and follow it.
